# EXP-482 review closure, before paid review

The compact packet contains the complete numeric design, not raw data or the
372-file inventory. Its inventory hash is reconstructed by `decision_context`
and the authentic review gate; source-qualified evidence is bound at
`ba85d90ba23ec44d4c6f66639d337812374719cf`.

| Claim submitted | Smallest controlling source and exercised surface |
| --- | --- |
| Only declared numerical/resource/random-draw changes | EXP-481/482 numeric designs; `tests/test_paired_successor.py` exact equality and seed reconstruction |
| Complete raw reference identity/order, not legacy spline extrema | `paired_inputs.py`, `paired_input_package.py`; source preflight raw audit and paired input tests |
| Same-cohort consecutive pairs, holdout and bootstrap | `paired_sampling.py`, `paired_replay.py`, `seed_return_map.py`; paired sampling/replay and seed return-map tests |
| All-case/variant turning regions and every event row, no coordinate rescue | `paired_decisions.py`, `paired_campaign.py`; complete cubic/two-sheet fixtures and bootstrap-failure tests |
| Distinct named attempt and exact review/source binding | `paired_release.py`, `paired_authorization.py`, three host controller/check/dispatch scripts; real Git/provider-tampering and actual-controller tests for both IDs |
| Full numeric configuration valid without target outcomes | Actual `run_paired_campaign.py --mode source --experiment-id EXP-482`; 426 source-bound tests, no skips; 128/64 full grid |
| Finer-step workload and failed original resource estimate | Source bdce7efe; `benchmark_paired_refinement.py` raw receipt; independently hash-bound read-only `summarize_exp482_workload.py` |

The source checker binds all tracked paired tests, numerical/script dependencies,
both numeric designs and original fixture manifests. Review brief/contexts and
future review/adjudication paths are reserved in the successor protocol. No
future reviewer response is needed to choose a path or update executable code.
No claim in the packet says the provider read or rederived the local sources.

The current canonical helper remains byte-identical to the previously vendored
version. Official documentation checked 2026-09-07 names
[gpt-6-astra](https://developers.openai.com/api/docs/guides/latest-model), with
[standard rates](https://developers.openai.com/api/docs/pricing) of $10 input,
$12.50 cache write and $50 output per million tokens. Its
[long-context threshold](https://developers.openai.com/api/docs/models/gpt-6-astra)
is above 272,000 input tokens, well beyond this compact packet and input-work
reserve. Use Pro mode with medium effort, 6,000 requested output tokens,
fivefold input/twofold output reserves, no tools, storage or researcher emphasis.

Reserve at most $2 for one new review, within the user's standing budget. The
packet includes the complete 16.8 KB prior review/structured adjudication as
required by the integrity playbook; no raw arrays or large source inventories
are submitted. Dry-run and non-generating exact token count must both fit the
declared bounds before generation. This guard is not a provider hard billing
cap. Do not retry a paid failure or submit another review for a better verdict.

The first local dry-run refused a combined historical/evidence context above
the canonical client's 12,000-character **per-context** cap, before any key or
API call. Keep the complete prior review in a separate byte-identical context
and the full structured adjudication with the new evidence; three contexts in
total, each within its own cap. No finding was dropped and no guard was raised.
Use 38,000 total input characters and 13,000 counted tokens with the same $2
reserve. The compact new brief is 3.2 KB; the additional historical material
is required cumulative-review context, not a raw-data or source dump.

The three-context dry-run passed: 36,699 complete input characters and $1.9762
reserved estimate under the $2 authorization (`artifacts/EXP-482/review-dry-03`).
It read official model documentation but did not load an API key, submit the
packet, count tokens remotely or generate a review. A sandbox-only attempt to
read that documentation failed DNS first; the allowed read-only network run
then succeeded. No paid review attempt occurred.

The final packet commit is blocked by the environment's public-disclosure guard.
It rejected publishing the separate prior-review context, then rejected again
after a read-only check established that its Git blob
`957524231ae2d14b06afafe90199e36a0a56be3a` equals the already-public
`origin/main:docs/reviews/EXP-481-review-01/review.md`. The file is our earlier
AI-generated advisory, not the professor's private review. Do not bypass the
guard or submit a review bound to a commit that lacks the final contexts.
Explicit approval of this public review-packet publication is now required.

The user subsequently explicitly authorized publishing this EXP-482 packet,
including the prior AI review, and instructed execution to continue. This
resolves the publication permission request, not the review/scientific gates.
The separate backup and heartbeat permissions are not prerequisites here.
Current official model/rate pages were fetched again: gpt-6-astra and the
standard 10/12.5/50 USD-per-million rates remain unchanged. A short context
addition discloses that the subsequent historical nominal-root audit did not
clear the missing bootstrap/array evidence; it changes no EXP-482 decision.
