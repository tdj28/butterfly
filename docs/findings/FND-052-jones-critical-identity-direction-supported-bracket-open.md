# FND-052 — Jones critical identity has a cross-coordinate direction but an open bracket

Status: directional evidence supported; strict identity not yet qualified

EXP-178 prospectively tracks the attracting Jones-section partition from the
two-branch to the three-branch regime. The last strictly resolved two-branch
row is `a=0.150` and the first strictly resolved three-branch row is `a=0.160`.
Both `x` and `z` uniquely match the old critical point to increasing-coordinate
critical `K1`, with normalized steps below `0.03` and runner-up margins above
`0.27`.

The experiment nevertheless fails because the resolved bracket width is
`0.010`, above the frozen `0.005` maximum. The interior points are retained as
oracle-disagreement gaps. The result supports a specific continuation
direction and rejects an arbitrary endpoint relabeling, but it does not yet
authorize renaming the neutral symbols to Jones's historical alphabet.

Evidence: [`../experiments/EXP-178-jones-critical-identity-path.md`](../experiments/EXP-178-jones-critical-identity-path.md).
