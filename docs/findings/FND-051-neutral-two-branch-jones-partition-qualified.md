# FND-051 — A neutral two-branch Jones-section partition is split-cloud qualified

Status: supported at `(a,b,c)=(0.11,0.2,20)`

EXP-177 applies the unchanged EXP-176 calibration/validation method to the
published unimodal parameter point on the distinct recovered Jones section.
All seven variants return two branches in both 1000-pair segments and in both
`x` and `z`; all variant consensuses are `1.0`. Joint critical drift remains
below the frozen bound in both coordinates.

Together with EXP-176, this completes the neutral dense-control pair required
by DEC-014 on one consistent historical representation. It does not yet say
whether the unimodal critical interval continues to the lower or upper
three-branch critical interval. That parameter-continuation identity is the
next gate before `C/D` or numerical branch labels can be attached.

Evidence: [`../experiments/EXP-177-jones-section-two-branch-control.md`](../experiments/EXP-177-jones-section-two-branch-control.md).
