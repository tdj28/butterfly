# FND-107 — The seventh daughter connects to the eighth event

Status: qualified by the combined EXP-326/327 evidence

EXP-325 retracts the approximate EXP-299 source interpretation, so connection
of the exact event-eight root to the immediate seventh daughter could no
longer be inherited from EXP-300--302. EXP-326 directly extends EXP-321's
4,096-step, 50-digit daughter by four more corrected pseudo-arclength rows.
The branch crosses EXP-306's independent event coordinate between stable and
unstable rows with signed transverse multipliers `-0.90215` and `-1.33360`.

At the target coordinate, fixed-`a` correction reaches `8.98e-23` matching,
retains primitive half-node RMS `7.9884935e-6`, matches the event period to
`3.13e-22`, and independently recovers multiplier
`-1.0000000000002505`. EXP-326 fails only because integer-node alignment
leaves `1.20e-6` RMS between meshes whose continuous phase hyperplanes differ
by `3.16e-6`.

EXP-327 preserves that failure and registers the bound event orbit on the
connected orbit's exact phase hyperplane. Two full Newton steps reach
`8.57e-26` matching. Direct same-phase node RMS falls to `6.35e-18`, period
difference is `1.02e-21`, and the independently transported multiplier is
`-0.9999999999999993`. Every frozen gate passes.

The immediate period-1536 daughter born at event seven is therefore the parent
sheet of event eight in the 4,096-step exact map. This restores the connected
eight-event finite cascade claim without relying on EXP-299. It does not prove
global sheet uniqueness, a ninth event, a limiting scaling law, paired shrimp
boundaries, TBA membership, double-criticality, a homoclinic connection, or
full-plane topology.

Tracked receipts: [`../experiments/receipts/EXP-326.json`](../experiments/receipts/EXP-326.json)
and [`../experiments/receipts/EXP-327.json`](../experiments/receipts/EXP-327.json).
