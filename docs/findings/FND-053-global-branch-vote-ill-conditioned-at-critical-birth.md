# FND-053 — Global branch voting is ill-conditioned at the shallow critical birth

Status: supported on the attracting Jones-section path

EXP-179 doubles observation and bootstrap support and resolves the lower
two-branch interval more deeply than EXP-178. It still leaves a structured
transition band in which oracle variants disagree about total branch count.
The change is coordinate-staggered: `z` first unanimously resolves three
branches at `a=0.1585`, `x` at `a=0.159`, and both do so at `a=0.1595`.

This is not a qualified critical identity. It is evidence that demanding a
single unanimous global branch count is the wrong continuation observable at
the point where a shallow extra extremum becomes detectable. The pre-existing
critical can instead be tested prospectively as a locally matched feature,
while detection of the new lower-coordinate critical remains a separate
branch-birth observable.

Evidence: [`../experiments/EXP-179-jones-critical-identity-power.md`](../experiments/EXP-179-jones-critical-identity-power.md).
