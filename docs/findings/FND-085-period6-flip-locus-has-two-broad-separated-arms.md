# FND-085 — The period-6 flip locus has two broad separated arms

Status: numerically qualified over the shared range `c in [7.01,8.25]`

EXP-217 passes all frozen gates at 135 exact period-6 real-`-1` events, extending
the returning arm from `c=7.00142` to `c=8.25273`. Every point retains seven
extremum-partitioned historical phases and eight Barrio phases, all adjacent
parameter jumps remain bounded, and the terminal event independently
recorrects under Radau.

Together with the original EXP-212 arm and the EXP-215/216 lower turn, this
forms one connected sampled event locus with two parameter-plane arms. Their
separation in `a` grows from `0.00891` at `c=7.16` to `0.05778` at `c=8.25`.

This is the strongest orbit-defined superstructure result so far. It is a
candidate skeleton for paired shrimp boundaries, but that interpretation is
not yet established. We must independently switch and qualify children on the
returning arm, determine the stable side at common `c`, and compare both arms
with the future TBA and double-critical sets.

Evidence:
[`../experiments/EXP-217-period6-flip-returning-arm.md`](../experiments/EXP-217-period6-flip-returning-arm.md).
