# FND-044 — A second transverse UPO skeleton and its seeds are qualified

Status: passed EXP-150 through EXP-152

## Finding

At `c=19.9`, the qualified finite saddle-topology bracket has recoverable
primitive unstable periodic skeletons at both endpoints. The two-branch
endpoint has three distinct recovered families (lags 3, 10, 13); the
three-branch endpoint has four (lags 13, 14, and two distinct lag-15 cycles).
All seven supply section-tangent unstable directions whose finite perturbations
reproduce the signed Floquet amplification.

## Implication

The orbit and local-manifold machinery is not confined to the original
`c=20` slice. This is necessary progress toward a topology surface and a
plane-wide explanation. It does not yet identify which families continue from
`c=20`, compare their lobes, or locate a manifold contact. Identity-preserving
continuation across `c` is mandatory before any cross-slice lobe claim.

Tracked receipts: `docs/experiments/receipts/EXP-150.json`,
`docs/experiments/receipts/EXP-151.json`, and
`docs/experiments/receipts/EXP-152.json`.
