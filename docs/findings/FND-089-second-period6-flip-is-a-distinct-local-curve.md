# FND-089 — The second period-6 flip is a distinct local curve

Status: numerically qualified over 21 exact points and three solver controls

EXP-227 continues the EXP-226 crossing through 21 exact coupled
orbit--real-`-1` events. Every point retains `7/8` two-section identity and
remains `5.60e-7--5.85e-7` lower in `a` than the independently computed
EXP-217 returning arm. Endpoint and center Radau controls agree with DOP853 far
inside every frozen threshold.

The second crossing is therefore neither an isolated coincidence of the
EXP-223 offset path nor a correction back to the already-known returning arm.
It is a distinct local period-6 flip curve. This is a major prerequisite for
paired shrimp-boundary anatomy, but it is not yet broad continuation, global
connectivity, a child-sheet closure theorem, the TBA, or a double-critical
center.

Evidence:
[`../experiments/EXP-227-second-period6-flip-local-curve.md`](../experiments/EXP-227-second-period6-flip-local-curve.md).
