# EXP-223 localizes a returning-child endpoint candidate

The full 52-event claim fails after a strong partial result: 212 accepted
points cover 45 exact returning-arm events through `c=7.62518642`. Two reached
independent controls pass.

Depth six then terminates in a `2.30e-4`-wide `c` bracket. The rejected root is
not a distant primitive child: it agrees with the parent state to `9.29e-8`
and closes after half its nominal period. EXP-224 will refine the intervening
parent real-`-1` crossing with DOP853 and Radau and test both sides directly.
