# EXP-151 passes; EXP-152 source hashes frozen

Date: 2026-08-07

The `c=19.9` lower endpoint contains three distinct primitive recovered UPO
families and the upper endpoint contains four. EXP-151 correctly reduces two
lag-6 double covers to the lag-3 family and retains two separate lag-15 upper
families.

EXP-152's adapter, family-selection rule, and all manifold-seed gates were
committed before EXP-150. Its manifest now adds only the EXP-150 discovery and
EXP-151 identity paths, hashes, identifiers, and the already declared required
case IDs. The numerical thresholds remain identical to EXP-142.
