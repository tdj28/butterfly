# EXP-239 passes; criticality audit EXP-240 is frozen

The period-24 candidate continues for all 20 frozen steps without correction
failure or loss of primitive `28/32` identity. Its half-node separation grows
by two orders of magnitude and the endpoint lies `8.14e-6` in `a` from the
event. This converts EXP-238's infinitesimal nomination into a separated orbit
branch.

The endpoint's preliminary multiplier near `-703` suggests that the child is
strongly unstable. EXP-240 now freezes a classification-neutral, fixed-
coordinate DOP853/Radau block-Floquet audit of both parent and child. Only that
receipt can establish whether the sampled flip side is subcritical.
