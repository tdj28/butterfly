# EXP-226 qualifies a second flip bounding the sampled child strip

Every frozen root, primitive-child, double-cover, identity, closure, stability,
and cross-solver gate passes. DOP853 and Radau localize the second parent flip
to within `3.96e-9` in `c` near `(a,c)=(0.2406843530,7.6253782976)`.

This is the strongest returning-arm child result so far: the stable primitive
period-12 strip persists through 45 exact source events and meets a second
period-6 flip along the fixed offset path. The next task is two-dimensional:
seed and continue that second event locus to determine whether it is a genuine
paired shrimp boundary and how it connects to the broad folded skeleton.

The result is now integrated into a new four-panel manuscript figure. The
rebuilt paper has 41 pages and 22 figures; all rendered pages pass visual QA.
The PDF SHA-256 is
`15e5036f84f5d6220e68c934b73a94643e7e245e6f0b5a045c21bd2efd4b7fd7`.
