# Update — EXP-149 association gates frozen before midpoint PIM

The EXP-149 evaluator and its tests are committed before EXP-148 runs. Its
binary relation is fixed: two branches require complete left-lobe exclusion;
three require at least ten lobe states on every line and horizon plus the
unchanged `5e-5`/`1e-4` fine/coarse distance convergence.

After EXP-148, only source paths and hashes may be inserted into the manifest.
This preserves the first genuinely prospective test of the lobe-inclusion
interpretation selected retrospectively by EXP-145.
