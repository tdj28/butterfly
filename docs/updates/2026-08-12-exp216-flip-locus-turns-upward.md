# EXP-216 finds an exact lower-`c` turn in the flip locus

The adaptive run rejects its frozen `c<=6.05` target. Instead, the exact event
curve turns near `c=6.83093` and returns upward through 21 accepted points to
`c=6.99993`, with unchanged `7/7/8` section identity and terminal Radau parity.

The stop is not dynamical: the next predictor lies just beyond the frozen
`c<=7.0` guard. EXP-217 expands only that guard and follows the returning arm
toward `c=8.25`.
