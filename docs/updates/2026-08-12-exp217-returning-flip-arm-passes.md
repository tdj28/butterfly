# EXP-217 qualifies a broad returning flip arm

The returning period-6 real-`-1` locus passes 135/135 accepted events and
reaches `(a,c)=(0.27126703,8.25273305)`. All points retain `7/7/8` section
identity, strict residual and adjacency gates, and terminal Radau agreement.

Across the shared `c` range, the returning arm separates progressively from
the original arm. This turns the local projection fold into a broad
orbit-defined two-arm skeleton. The next decisive test is not more curve
drawing: it is independent period-12 branch switching and stability exchange
at held-out returning-arm slices.
