# EXP-184 historical alphabet test frozen

Date: 2026-08-07

The Jones author source and EXP-183 together determine the critical labels:
`K1 -> C` and `K0 -> D`. The source also gives a target-word-blind geometric
route to the numerals: `B0 -> 2` is the newly opened outer branch beyond `D`,
`B2 -> 0` is predicted to be its innermost deposition interval, and the
remaining `B1 -> 1`.

EXP-184 freezes that mapping before evaluating it. Two fresh DOP853/Radau
trajectories, two disjoint segments, both scalar coordinates, strict physical
inner-distance separation, transition support, and cross-coordinate label
agreement are required. No Figure 6 target word or landmark orbit enters the
test.
