# Update — EXP-132 higher-horizon transverse brackets preregistered

EXP-131 falsified a nearly vertical continuation at `c=19.8` and selected a
new upper endpoint at `a=0.150`. EXP-132 freezes the resulting two finite
brackets at a 256-return censor ceiling before generating target state.

The exact coverage-only censor independently qualified in EXP-121 is now a
reusable return-map result with direct unit tests. Strict oracle output remains
alongside the censored decision in every receipt. The rule cannot excuse
bootstrap instability, a contradictory branch count, noncoverage failure, or
critical-point drift.

A full pass would establish finite brackets `[0.148,0.150]` at `c=19.8` and
`[0.145,0.150]` at `c=19.9`. It would still leave continuous curve
continuation and the unstable-orbit/manifold event open.
