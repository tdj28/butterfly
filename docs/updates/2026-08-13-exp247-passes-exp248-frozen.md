# EXP-247 passes; the period-96 bracket scan is frozen

The primitive period-48 child continues through all eight frozen steps,
retains exact `56/64` identity, and reaches a preliminary real multiplier of
`-29.237`. The near-event child is independently stable, so a subsequent
stability crossing lies on the sampled branch if the multiplier identity is
continuous.

EXP-248 freezes a magnitude-separated block-Floquet scan of all nine rows,
using the corrected identity rule before inspecting this branch's spectra.
