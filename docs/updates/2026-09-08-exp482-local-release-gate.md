# EXP-482: replacing the paid-review prerequisite, not the scientific gates

The explicit human policy now has an executable local-audit path scoped only to
EXP-482. This is an administrative amendment before target outcomes. The numeric
plan remains byte-for-byte unchanged (SHA-256
`92c989f734afb36cb539f35c0ba0242e5459202b4142ba462e12c223dcce5d8a`).

The local gate pins that plan and the policy independently of the submitted
release, binds all source/test paths to the tested/pushed code freeze, retains
the original review/adjudication, failed EXP-481 numerical result, EXP-482
workload evidence and quota failure, and requires an explicitly labeled local
audit. It never invokes the provider helper. The former reviewed path remains
available for historical validation; it is not used to invent an EXP-482 review.

The API refill did not authorize another request. No paid request, target
trajectory, upload or new cloud worker has occurred in this migration.

Fresh setup still runs the complete source-bound tests, independently extracts
the hash-bound raw reference inputs, validates all configurations and realizes
the sealed runtime. The worker still checks its actual parent/controller bytes.
The existing one-shot slot and all phase predecessor/completion checks remain.
Both cases and every accuracy, support, retention and bootstrap criterion remain
unchanged. Existing EXP-481 evidence and consumed marker are untouched.

New grants identify the binding honestly as `release_sha256` plus
`release_mode`; no field claims a provider response was obtained. Old persisted
grants are not rewritten or promoted to new authority.

Initial verification: 89 focused tests passed, including real temporary Git
repositories and 23 new local-release/dispatch/grant cases. They reject changed
plan/policy/source, missing context, fake response fields, wrong experiment,
uncommitted audits, source-only setup and mismatched release modes. Another 107
numerical and actual controller/worker tests passed, including OS-level parent
checks and negative grants. Fresh source preflight is still required and not
yet claimed complete here. A release candidate is not execution authority.

The complete local suite subsequently passed **1,781 tests**, with one existing
Linux-only process-identity test skipped on macOS (110.54 seconds). The staged
public-repository scan passed for 2,470 tracked files. A read-only replay of the
full-workload summary reverified its original hashes and file inventory: the
original .25-second feasibility gate remains failed, and the separately labeled
one-second projection fits. No benchmark or research trajectory was repeated.

Scientific scope remains conditional finite-resolution map adequacy on the
reference-conditioned retained cohort. Jones's flow-level chains, exact
criticality and homoclinic existence remain unresolved.
