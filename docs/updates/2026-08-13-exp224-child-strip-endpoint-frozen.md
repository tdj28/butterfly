# EXP-224 child-strip endpoint test is frozen

The successor to EXP-223 now separates three possibilities with direct gates:
a primitive stable child immediately before the candidate boundary, an exact
period-6 real-`-1` crossing reproduced by DOP853 and Radau, and a stable parent
plus its double cover immediately afterward.

The test follows the exact EXP-223 offset path. It cannot by itself promote the
localized crossing to a global child-sheet endpoint or paired shrimp boundary.
