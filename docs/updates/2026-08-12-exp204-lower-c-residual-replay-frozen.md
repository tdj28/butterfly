# EXP-204 freezes the lower-c residual replay

EXP-203 preserves 551 individually qualified stable period-6 orbits but does
not close the search domain. EXP-204 now freezes fresh GPU trajectories and the
unchanged EXP-202 three-level smoothing ensemble over every one of those
orbits. A candidate must agree across 12 scale/support/step reconstructions;
the experiment requires at least 250 eligible candidates before a direct point
or strict two-residual cell can be nominated.

The 867,378-byte candidate artifact is generated and hash-bound. The separate
stability/fold and unstable-family continuation remains on the execution list
regardless of this residual replay's outcome.
