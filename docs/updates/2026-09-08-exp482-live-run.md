# EXP-482 clears numerical qualification; full collection is running

**Completed-run update:** collection and analysis have now ended cleanly.
Both primary maps are unresolved because of insufficient supported domain.
See the [complete result and eight-panel figure](2026-09-08-exp482-map-result.md).
The live-run account below is the earlier qualification checkpoint; do not
monitor or rerun EXP-482 as an unfinished experiment.

**Meaningful progress:** all **192 of 192** numerical comparisons passed, 96/96
in each nominated case. The finer RK4 steps cleared the unchanged 1e-4
state/time prerequisite that stopped EXP-481. Independent reconsumption of all
128 saved trial journals reproduced every recorded comparison exactly. This
uses the same comparison implementation on the raw data, not a second solver
implementation or a proof of correctness.

The controller verified the completed phase and advanced automatically to full
collection. Collection and map analysis are not yet complete at this checkpoint.
Jones's symbolic chains and homoclinic claim remain unresolved: this result
qualifies the numerical prerequisite, not the substantive symbolic explanation.

## What passed

| Solver pair | Passed / 32 | Maximum scaled event-state difference |
| --- | ---: | ---: |
| RK4 .0025 / RK4 .00125 | 32 | 2.51630e-6 |
| RK4 .0025 / DOP853 | 32 | 2.63520e-6 |
| RK4 .0025 / Radau | 32 | 2.63069e-6 |
| RK4 .00125 / DOP853 | 32 | 1.46961e-7 |
| RK4 .00125 / Radau | 32 | 1.43729e-7 |
| DOP853 / Radau | 32 | 2.78204e-8 |

The largest time difference is 4.01961e-9; raw/accepted event counts and capture
memberships agree. The qualification worker completed in 90.54 seconds with
verified process cleanup and 123,469,824 bytes peak sampled RSS. These are
short-horizon diagnostic comparisons, not long-time shadowing or rigorous error
bounds. EXP-482 uses a fresh, already frozen initial-state draw, so comparison
with EXP-481's maxima is **not** a same-trajectory convergence experiment.

The [complete qualification receipt](../experiments/receipts/EXP-482-qualification-result.json)
retains every solver-pair group, both case counts, original hashes and observed
supervisor data. `scripts/summarize_exp482_qualification.py` reopens only the
completed qualification and input evidence; it never integrates, retries,
uploads or snapshots the changing collection tree.

## Execution and policy

Exact execution freeze: `8ce37169ef9c24c570f5e2b9d5a60e48f310c377`, preserved on
`codex/exp482-local-execution`. Code freeze for the local audit:
`e81466a0a18227e91682c68dc63bcf29ce76d688`. The
[actual local audit](../reviews/EXP-482-local-audit.md) and release were pushed
before any new target trajectory. The real release check passed (811 bytes,
SHA-256 `1c94d77bd76d94f925357967dba1cefaf6b6452d368abc81e3700c1e9c3b36fc`).

Fresh production setup passed **449 source-bound tests, zero skips** in 96.29 s,
both raw reference audits and all 192 fixed configurations (128 qualification
trials, 64 collection batches). Its receipt SHA-256 is
`d74ae87ec66cb9ee52204c31f3f3ca78ea173231206db0847f040735177aeeb7`.
The qualification phase terminal is independently anchored at
`6b3cd52dd120a299b1d59d4d7d34153f7a77d50d8a55bb4aec75e37e3c6e5724`.

The live command was:

```sh
/usr/bin/caffeinate -i .venv/bin/python -B scripts/run_paired_campaign.py \
  --mode execute --release-mode local-audited --experiment-id EXP-482 \
  --source-commit 8ce37169ef9c24c570f5e2b9d5a60e48f310c377 \
  --remote-ref refs/heads/codex/exp482-local-execution \
  --output-dir artifacts/EXP-482/target-8ce3716
```

**Do not rerun this command.** `artifacts/EXP-482/target-once.json` is consumed.
The active run is `artifacts/EXP-482/target-8ce3716`; preserve every phase outcome,
cap failure and raw prefix. EXP-481's marker and stopped evidence remain intact.
No paid API request, new cloud host or upload was used for this run. The failed
Pro attempt remains historical; it is not retried or represented as approval.

Results documentation is maintained on a separate branch so the execution ref
and running controller bytes stay unchanged. PR52 was normally squash-merged
after all four Python 3.12/3.13 CI checks passed, at remote main commit
`a439677e665f6f997564e4b8f4f6fbe08cd65927`. The exact head was matched during
merge; no automatic merge was used as a check barrier. The results checkpoint
is separate from the immutable execution ref.

## Reporting check and next step

The first direct invocation of the new read-only auditor failed with an import
path error before reading target evidence. Direct-file package lookup was fixed
and given a regression test. All six combined old/new reporting tests passed,
and the actual raw replay then completed exactly. The research controller was
unaffected and never restarted. Neither a reporting failure nor a successful
raw replay is a new experiment attempt.

Next: let the existing bounded collection complete, then let the frozen
controller assess both cases using all prespecified map/retention/support and
bootstrap criteria. Report inadequate support, operational failure or mixed
cases without rescue or reinterpretation. A positive map result would still
leave the flow-level symbolic-chain connections to be tested explicitly.
