# EXP-185 qualifies the operational Jones alphabet

Date: 2026-08-07

EXP-185 passes from clean commit
`ecd64f41f2c7d0cb0604fd797f1691d18104524e`. Its raw receipt SHA-256 is
`ce6439ba9b196c4ca4d535c825969882e48d5f6852c2d8e9dbd39fa75783c877`.

Both solvers, both disjoint segments, and both scalar coordinates pass. The
innermost interval is always `B2`, 96--112 `B0 -> B2` deposition transitions
occur in every row, no sampled `B0 -> B0` transition occurs, and all four x/z
agreement fractions are exactly one. The operational mapping is therefore
qualified as `K1/C`, `K0/D`, `B0/2`, `B1/1`, `B2/0`.

The next gate is to encode independently corrected Figure 6 landmark orbits
under this immutable partition and mapping. Target words may now be compared;
they still may not be used to move a boundary, permute the alphabet, or select
an orbit.
