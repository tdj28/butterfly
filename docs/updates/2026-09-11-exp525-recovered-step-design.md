# The approved recovery advances to one fresh step

The retained-data audit has completed. Its detailed receipt remains private
pending explicit approval for public release; it is not being repackaged or
uploaded by the successor. The evidence already resides on prax and can be
consumed there under the human-approved research scope.

[EXP-525](../experiments/EXP-525-recovered-single-step.md) implements the
conditional second phase of that same recovery: one fresh predictor and at
most one normal correction, using explicitly historical calibration. The
source and plan are public; detailed inputs remain access-controlled. Public
CI tests synthetic numerical controls and sealed source startup. The
evidence-host startup additionally authenticates the actual private receipt.

The combined runtime ceiling is six hours for collection and its raw audit.
No numerical tolerance, root identity or historical/Barrio event-count gate
is relaxed. No new GPU rental or paid API request is made. A local controller
test caught a deployment-template count mismatch before target execution;
the exact branch/ref substitutions are now checked explicitly.

This update records implementation and authority, not a newly measured step.
Jones's flow-level symbolic chains remain unverified. The scientific question
is whether the next actual measurement agrees with the calibrated prediction.

The final combined local suite passes 83 tests, including synthetic collection
through the actual writer and polynomial-census auditor, failure retention,
isolated public-source startup, and the separately exercised private-calibration
startup. Paper reference checks pass. A separate local adversarial review found
no blocker in the one-step controller, historical/new accounting, fixed gates,
private-input boundary or six-hour limit. No paid review was requested.
