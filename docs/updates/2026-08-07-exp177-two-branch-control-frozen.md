# EXP-177 two-branch Jones-section control frozen

Date: 2026-08-07

EXP-176 qualifies the neutral three-branch partition at `(0.2,0.2,20)` in
split-cloud `x` and `z`. DEC-014 still requires the corresponding two-branch
control before any neutral symbol is mapped onto Jones's alphabet.

EXP-177 is now frozen at the published unimodal parameter point
`(0.11,0.2,20)`, but deliberately uses the distinct recovered Jones
negative-oriented half-plane. It predicts two neutral branches in both
coordinates. Pair counts, guard gap, seven oracle variants, 100 bootstraps,
solver, coverage, graph-likeness, consensus, and critical-drift thresholds are
unchanged from EXP-176. Passing will qualify the control on this representation;
it will not establish equivalence of the Jones and Barrio sections.
