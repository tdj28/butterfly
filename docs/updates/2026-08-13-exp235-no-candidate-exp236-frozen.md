# EXP-235 has no candidate; EXP-236 targets the closest trial

The period-24 singularity and tangent separation pass, but all 12 full-period
switch trials fail. The positive `0.00025` trial is closest at residual
`1.19e-5` after exhausting 160 evaluations. EXP-236 preserves that predictor
and every science gate while raising only the corrector ceiling to 480.

If it still fails, the next representation is segmented multiple shooting;
further undirected full-period scale ladders are not justified.
