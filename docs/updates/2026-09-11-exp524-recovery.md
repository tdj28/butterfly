# EXP-524: approved audit of the interrupted calibration

The human approved recovery on 2026-09-11: up to twelve hours auditing the
retained results, followed, only if qualified, by one separately frozen
verification step capped at six hours on the existing prax host. No paid API
request or new GPU rental is authorized. These are hard execution ceilings,
not completion-time estimates.

The [machine-readable recovery ledger](../../experiments/manifests/EXP-524-recovery-cycle.json)
records the approval, failure identity, new C/E/F lineage and single audit
attempt. This ledger is committed before successor implementation and outcome
access. The audit implementation and tests must be separately frozen before
the numerical replay starts.

EXP-523 remains incomplete. EXP-524 will replay every complete calibration
point, covering both solvers, all fold constructions, both cycle windows and
the complete stationary-root census. It will separately inventory the
interrupted predictor, without using it as a successful measurement. No new
integration is permitted in this audit.

The runtime failure was an underestimated collection budget. Recovery avoids
repeating eight expensive calibration points only if their retained evidence
passes the unchanged raw checks. A future one-step experiment must explicitly
cite reused data as historical inputs and retain all newly attempted outcomes.
Nothing here verifies a Jones chain, identifies C/D, or proves a homoclinic
connection.

Current stage: implementing the bounded audit and its operational checks.
The last scientifically audited result remains EXP-522. No recovery result
or new continuation measurement exists yet.
