# EXP-524: approved audit of the interrupted calibration

The human approved recovery on 2026-09-11: up to twelve hours auditing the
retained results, followed, only if qualified, by one separately frozen
verification step capped at six hours on the existing prax host. No paid API
request or new GPU rental is authorized. These are hard execution ceilings,
not completion-time estimates.

The [machine-readable recovery ledger](../../experiments/manifests/EXP-524-recovery-cycle.json)
records the approval, failure identity, new C/E/F lineage and single audit
attempt. This ledger is committed before successor implementation and outcome
access. The audit implementation and tests must be separately frozen before
the numerical replay starts.

EXP-523 remains incomplete. EXP-524 will replay every complete calibration
point, covering both solvers, all fold constructions, both cycle windows and
the complete stationary-root census. It will separately inventory the
interrupted predictor, without using it as a successful measurement. No new
integration is permitted in this audit.

The runtime failure was an underestimated collection budget. Recovery avoids
repeating eight expensive calibration points only if their retained evidence
passes the unchanged raw checks. A future one-step experiment must explicitly
cite reused data as historical inputs and retain all newly attempted outcomes.
Nothing here verifies a Jones chain, identifies C/D, or proves a homoclinic
connection.

Current stage: **the audit is running on prax**. The audit implementation passes 66 focused recovery and parent
regression tests, including an isolated physical-source startup. Paper reference
checks pass. A separate local adversarial review found no blocker in the audit,
inventory partition, no-integration guard, new runtime metadata or timeout
controls. Its review accessed no target scientific values and made no paid call.

The first combined test run exposed unrelated pytest imports entering the
development-process source inventory. The test now checks the actual isolated
deployment process instead; the production closure check remains unchanged.
This was a pre-outcome test-harness correction, not a failed scientific attempt.

The immutable audit source was published, the authentic same-host preflight
passed, and the one-shot twelve-hour audit started. The existing prax environment was
checked: Python 3.13.13, NumPy 2.5.1 and SciPy 1.18.0, with approximately
19 GiB free. The audit reads the old files in place and reserves only its small
new receipts; it does not duplicate the full raw data.

The last scientifically audited result remains EXP-522. No recovery result
or new continuation measurement exists yet.

## Verified execution

- Frozen source: `da58089507c29b9299aa6b75e23723d037576e06`, publicly verified at
  `refs/heads/codex/exp524-prax-execution` before launch.
- Plan SHA-256: `dced8cf33804ec30380efe273aff6d3df82a0a2a3af6369bc052b3f4e53527eb`.
- The new sparse checkout contains all 214 required physical files. The old
  numerical interpreter is reused without editing the old source or environment.
- Started: `2026-09-11T18:35:49.942111+00:00`. Hard audit interval: 43,200 seconds,
  ending at approximately `2026-09-12T06:35:50+00:00`.
- A separate post-launch SSH check confirmed a live audit process in its exact
  new checkout, plus matching new binding and consumed-attempt marker files.
- New products: `artifacts/EXP-524/audit-01/` in the task-owned prax checkout.
  No new raw trajectory is generated; the old EXP-523 files remain in place.

PR 91 tracks the approved recovery ledger and tested audit implementation.
Execution progress is operational evidence only. Interpret scientific values
only after the audit finishes and its receipt and original-byte preservation
checks pass. The conditional six-hour successor has not been launched.
