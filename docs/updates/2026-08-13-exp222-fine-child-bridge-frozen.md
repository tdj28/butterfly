# EXP-222 freezes a fine bridge across the first child-root jump

The closest qualified child to the returning flip event is selected by exact
receipt identity. Sixteen equal parameter substeps replace EXP-221's single
coarse jump. No scientific gate changes: the child must remain primitive and
stable, the parent unstable, identities `7/8` versus `14/16`, and three
independent solver controls must pass.
