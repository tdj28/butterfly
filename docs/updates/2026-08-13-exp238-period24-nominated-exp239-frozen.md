# EXP-238 nominates period 24; EXP-239 is frozen

The representation change succeeds. The exact EXP-237 anti-periodic mode
produces two passing, primitive period-24 candidates with `28/32` section
counts and half-period closure near `5.02e-4`; EXP-236's doubled-parent collapse
is absent. Both signs remain extremely close to the event in parameter space,
so their preliminary stability is not yet promoted to a finding.

EXP-239 freezes 20 segmented pseudo-arclength steps along one sign, requiring
a separated endpoint without relaxing matching, primitivity, or section
identity. Independent two-solver and sign-equivalence qualification follows
only if this continuation passes.
