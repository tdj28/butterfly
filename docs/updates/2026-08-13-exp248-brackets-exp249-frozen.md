# EXP-248 brackets period 96; EXP-249 is frozen

The nine-row magnitude-separated Floquet scan passes and isolates one
period-48 real-`-1` bracket. The tracked multiplier moves from `-0.507037` to
`-2.256540` across a `2.36e-8` interval in `a`.

EXP-249 freezes the exact 64-segment augmented event solve, including
independent Radau, primitive section identity, and every proper-subperiod gate.
