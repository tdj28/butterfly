# EXP-481: final source qualification and prospective review

The complete campaign controller is merged on `main` at
`2f05fd2fb96e1ee7329010a8828e85e86f783db4` (PR47, all four Python 3.12/3.13
checks passed). The new `codex/exp481-execution` branch starts there.

## What this advances

We can now run the planned experiment through one controller: verify source
and inputs, check numerical event accuracy, collect the entire fixed cohort,
then analyze the complete evidence. It refuses incomplete predecessors and
does not automatically retry or refill an unfavorable sample. This is research
infrastructure, not a new confirmation of Jones's symbolic dynamics.

On this execution branch, the genuine source-bound control passed again:
364 source/test-support files, **379 tests with no skips**, both preserved
reference audits and all 640 configuration checks. All three analytic-circle
phases completed; both identity maps resolved. No research trajectory was
generated and the target attempt remains unused.

Local evidence is retained in
`artifacts/EXP-481/execution-source-control-02`:

| Receipt | Bytes | SHA-256 |
| --- | ---: | --- |
| `preflight/receipt.json` | 10,887 | `2db3cfc86175bd094ac5fb7c8198eb3961c89184eafced370fbd101f9f57b485` |
| `campaign/receipt.json` | 102,976 | `2752d7d19b5ad23aded5421497e913d22520afd4acc471375cefebaffe5c47dd` |

The first control used a mistyped commit ID and failed before scientific work.
Its evidence remains in `execution-source-control-01`; the corrected command
used a fresh directory and did not change code or scientific thresholds.

## Review checkpoint

The exact compact packet is pushed at
`c8f4bc322f4e81aa94821bd1c2bc0435cc403542`. The
[review brief](../reviews/EXP-481-review-brief.md) explains the research question;
the [decision context](../reviews/EXP-481-decision-context.md) contains the full
numeric design and source/design inventory commitment. The
[closure note](../reviews/EXP-481-review-closure.md) maps claims to local code,
tests and evidence and reserves review/release roles without placeholder approvals.

Two local cost checks rejected the packet before network generation ($1.2770
and $1.2540). Only redundant explanatory prose was shortened; the complete
numeric design was unchanged. The exact committed packet passed dry-run at
17,261 input characters and a **$1.2473 conservative reserve**, under the $1.25
authorization. That reserve is not an enforceable provider billing ceiling.

One GPT-6 Astra Pro/medium review completed with **READY AFTER SPECIFIED FIXES**
at a reconstructed cost of **$0.5888**. Its exact promoted bundle passes the
canonical offline validator. The two blockers ask for workload/headroom
estimates and full positive-turn/adverse-path controls; three important findings
clarify reference conditioning, legacy-input dependencies and outcome reporting.
The [completed adjudication](../reviews/EXP-481-adjudication.md) records the
accepted fixes and frozen synthetic benchmark. No second paid review was made.
Both blocking evidence gates now pass; the final source qualification passed
**381 tests with no skips** and all three analytic phases. The benchmark's
doubled estimates fit the unchanged limits, with collection estimated at
5,647 seconds against its 14,400-second bound. This is a workload extrapolation,
not a promise about actual target completion time.

The [machine release](../../experiments/manifests/EXP-481-reviewed-release.json)
binds the complete reviewed and final inventories, exact response, every
finding/change mapping and final code freeze
`68585e2bf853fcd2778b6efdf4b2e652bb6749db`. It must pass the actual pushed-release
gate and fresh production setup before the single target attempt can begin.

## Scientific boundary

This experiment asks whether an adequate historical-x return map exists on the
specified retained finite-time population, and whether the nominated cycles'
section events lie near its estimated turning regions. Even a positive result
would not yet verify Jones's alphabet, chain arrows, exact criticality or
homoclinic assertion. The separate legacy stationary-inflection impact audit
also remains open; no old claim is cleared by the new analyzer's controls.

Next: publish and verify the exact release, then execute the bounded experiment
without another routine permission request. Preserve this execution branch
until its ancestry-dependent release checks and campaign are complete.
