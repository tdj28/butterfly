# EXP-220 qualifies local opposing-side children on the returning arm

The three-slice experiment fails globally but yields a decisive local pass at
its untouched near slice. Four lower-`a` primitive stable period-12 children
pair with unstable period-6 parents and pass period, section, primitivity,
Floquet, and DOP853/Radau whole-orbit gates.

The middle and far switches remain unresolved. The next step is identity-safe
continuation of one qualified near child along the returning event arm, which
tests persistence without requiring a fresh ill-conditioned branch switch at
every remote slice.
