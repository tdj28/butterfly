# EXP-227 local second-flip continuation is frozen

The second EXP-226 crossing is now promoted from a one-path endpoint to a
prospective 21-point local event-curve test. Every exact coupled event gate is
retained, source-arm separation is measured and gated at each point, and
Radau controls are frozen at both ends and the center.
