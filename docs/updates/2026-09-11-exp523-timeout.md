# EXP-523 stopped at its wall limit; the evidence is preserved

**Status: incomplete, not scientifically audited.** This is a computation-budget
failure, not a negative finding about Jones's symbolic chains.

The prax worker stopped at `2026-09-11T04:22:18.280192+00:00`, reporting
`BudgetStop: EXP-523 wall deadline`. The recorded interval from its start is
21,600.041341 seconds. The six-hour collection budget underestimated this
workload on prax; it was not extended after launch. No process was restarted,
no marker reset, and no numerical threshold changed.

## What completed, and what did not

- All eight first-step calibration points completed: four in a and four in c.
  The operational log records 864 IVP calls through their completion.
- The first predictor started but was interrupted during an integration.
  The failure receipt records **875 IVP calls started**, not 875 successfully
  completed or audited integrations, and 667,500 periodic census segments.
- No complete continuation endpoint was produced. There is no `summary.json`
  and no successful `primary-audit-01.json`. The worker calls the complete
  auditor only after successful collection, so that audit did not run.
- Scientific metrics from these points remain uninspected. Completion of a
  calibration point is not a claim that it passes independent raw replay.
  Neither successful continuation nor a failed mathematical prediction follows.

## Preservation check, separate from a scientific audit

The read-only [inspector](../../scripts/inspect_exp523_failure.py) authenticates
the failure, binding, consumed marker, log and manifest against their recorded
SHA-256 values. It then checks every retained file against the failure inventory
and all physical frozen source/input files against their bound hashes. It never
imports numerical code, runs an integration, modifies remote files, or interprets
the saved scientific outputs.

The [preservation receipt](../experiments/receipts/EXP-523-timeout-preservation.json)
records:

- 1,299 inventory entries matching all recorded byte counts and hashes;
- 3,241,861,585 inventory bytes, plus the 276,312-byte failure receipt:
  **3,242,137,897 total retained run bytes**;
- all 206 frozen physical source/input files unchanged;
- `complete_raw_audit: false`, `new_integrations: 0`, and
  `symbolic_chains_verified: false`.

The inspector's 15 local controls pass, including missing/extra/changed files,
symlinks, escaping paths, and the published receipt's exact identity and scope.
The combined inspector/controller/runner suite passes all 41 tests; paper
reference checks also pass. A separate read-only local review found no blocker.
This is a preservation audit, not numerical replay or independent replication.

Frozen source: `eae6745d124c2fa59efc4eff337929e9ff71607d`.
Failure SHA-256: `3816d1351a8efc5c2fa2610118fbab1fe40989acfb7c993850be4b7b750c5116`.
Preservation receipt SHA-256: `0c440f75221792c55b5c83b0dcc2c7175dbd9a9057bbc32fb380ed4c3783a19a`.
The immutable execution ref and consumed marker remain in place.

The complete raw run remains on prax at
`/home/ubuntu/butterfly-research/exp523-refreshed-eae6745d124c/source/artifacts/EXP-523/target-eae6745d124c`.
Only the small failure/binding/marker/log diagnostics and preservation receipt
were copied locally to `artifacts/EXP-523/timeout-diagnostics-01/`.
**No second complete raw backup or public raw release has been established.**
No new paid API call or rented worker was used.

To repeat only the read-only preservation check, choose a fresh local output:

```sh
.venv/bin/python -B scripts/inspect_exp523_failure.py \
  --output artifacts/EXP-523/timeout-diagnostics-01/preservation-receipt-02.json
```

## Recovery boundary and proposed next work

The canonical research-integrity policy §12 classifies this as **Tier C**:
target data were computed, even though the scientific outputs remain uninspected.
It requires new human recovery authority. The earlier instruction to continue
does not silently reset this consumed attempt or retroactively extend its cap.
No replacement run is authorized by this incident record.

Proposed recovery, **pending approval**:

1. Freeze a read-only partial-run audit that verifies all eight complete points
   and separately inventories the incomplete predictor. Do not fabricate a
   successful EXP-523 summary or omit its failure.
2. If that audit qualifies the completed calibration data, bind them explicitly
   as historical inputs to a separately named, outcome-informed successor.
   Keep old and new raw data and claims distinct; never present reused calls as
   fresh measurements or the successor as the original run finishing.
3. Test one fresh predictor/corrector step with the unchanged scientific gates,
   a new source freeze, attempt and namespace, and a realistic operational
   budget. Preserve all failure outcomes and do not promote a sampled step to
   grazing, a homoclinic connection, C/D identification or a Jones arrow.

The initial proposal is up to twelve hours for the recovery audit and six
hours for one-step collection, on the existing prax host, with no paid Pro
request or new GPU rental. This is a proposed ceiling, not a completion-time
estimate or launch authority. Before any new collection, a tested storage
budget must fit the verified free space while preserving every old raw file.
At failure inspection prax had about 19.0 GiB free, below the old runner's
20 GiB initial-space requirement; blindly restarting that runner would fail
its own preflight. The Mac also lacks comfortable space for a full raw copy
while preserving the existing 8 GiB reserve. No unrelated data may be deleted
to make room.

Jones's flow-level symbolic chains therefore remain **unverified**. The next
scientific decision must come from audited geometry and new measurements, not
from this timeout or from passing the preservation checks.
