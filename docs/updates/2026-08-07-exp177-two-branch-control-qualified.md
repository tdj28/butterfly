# EXP-177 neutral two-branch control qualified

Date: 2026-08-07

EXP-177 passed from pushed clean commit
`49f9e9495b161b3d8746891dea28db25a0a5a0c6`. The published unimodal parameter
point produced 2582 crossings on the distinct recovered Jones negative
half-plane. All seven variants resolve two branches in calibration and
held-out validation for both `x` and `z`, with consensus `1.0` throughout.

The raw receipt is frozen at SHA-256
`37a19c52715792553f7a61383bd4df9172080ffbfc965a41b1dc601daf10228f`.
The neutral two/three control pair is complete. The next experiment must track
critical identity across parameter space rather than choosing the endpoint
critical interval closest to the unimodal value.
