# EXP-227 qualifies the second flip as a distinct local curve

All 21 exact events and three Radau controls pass. Across the entire frozen
neighborhood, the new curve remains roughly `5.7e-7` lower in `a` than the
known returning arm, so this is not source-arm root substitution.

EXP-228 will now continue both ends by pseudo-arclength toward
`c<=7.46` and `c>=7.78`, retaining exact event and source-separation gates.
