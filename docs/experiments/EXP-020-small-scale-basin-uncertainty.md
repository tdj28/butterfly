# EXP-020 — Prospective small-scale basin uncertainty

Status: executed; prospective small-scale fit passed
Manifest: `experiments/manifests/EXP-020-small-scale-basin-uncertainty.json`
Claim target: CLM-019

## Purpose

Test prospectively whether the small-scale exponent suggested by EXP-019
persists when the scale window is extended four dyadic levels. EXP-019 showed a
coarse saturation plateau and a post-result four-scale slope near 0.373. This
experiment freezes the entire new window before execution and fits all seven
declared scales, avoiding post-hoc window selection.

## Frozen method and gate

The parameters, initial-condition plane, pair geometry, Float64 GPU integrator,
transient, observation horizon, and recurrence classifier are unchanged from
EXP-019. The seven epsilons run from 0.125 to 0.001953125. Two new deterministic
seeds and 4,096 pairs per seed give 8,192 pairs per scale and 57,344 pairs in
total. The execution gate again requires at least 98% of pairs at every scale
to resolve into period 3 or period 12.

The primary numerical result is the all-seven-scale log-log slope, its 5,000-
sample pair-bootstrap interval, and `R^2`. Consistency with the EXP-019
small-scale candidate requires a well-fit positive exponent in roughly the
previous `[0.35,0.40]` band. Failure of that comparison is retained and would
mean the previous four-scale window was pre-asymptotic or unstable.

## Limits

Even a stable result applies only to the declared plane and basin pair. A final
fractal-boundary claim still requires CPU subset parity, integration-horizon
sensitivity, added spatial regions or conditioning rules, and assessment of
direction/seed dependence.

## Result

The frozen NVIDIA L4 run at commit
`7b11a1a5892b413ed4a85b8b335bb11b5bb50aea` completed in 130.93 seconds.
It resolved 57,342 of 57,344 pairs into period 3 or period 12; every scale
exceeded the 98% execution gate. The uncertain fractions declined monotonically
from `0.3236` at epsilon `0.125` to `0.05657` at epsilon `0.001953125`.

The prospectively declared all-scale fit gives:

- `alpha = 0.42643`;
- pair-bootstrap 95% interval `[0.40942, 0.44418]`;
- `R^2 = 0.99759`; and
- slice-boundary dimension estimate `2-alpha = 1.57357`.

The receipt SHA-256 is
`c6cc78bab8f227cf41b8225c17ccaa9b4802f37ddf211617f633b7c524698473`,
matching the remote file exactly. The EXP-020 pod was terminated and is absent
from the account list. A separately named `ndl-lyapunov-20260806` pod appeared
after teardown and was deliberately left untouched because it is outside this
experiment.

The combined EXP-019/020 log-log figure is generated by
`scripts/plot_basin_uncertainty.py` and retained at
`artifacts/EXP-020/EXP-019-020-basin-uncertainty.png`; its SHA-256 is
`43d16d6f171dda077af76be9fe0ce80928eb85e46d874cb5b38363ef2e556ad1`.

## Decision

The preregistered result confirms a nonzero, sub-unity uncertainty exponent
with an excellent power-law fit across a 64-fold scale range. For this declared
initial-condition plane, “numerically supported fractal basin boundary” is now
justified; “riddled basin” is rejected at the measured scales because the
uncertain fraction decays with a clearly positive exponent.

The numerical dimension is still provisional. EXP-019's adjacent coarser
small-scale window produced a lower post-result slope near 0.373, so a final
dimension requires scale-window convergence, CPU subset parity, longer-
transient sensitivity, and spatial conditioning.
