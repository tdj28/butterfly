# EXP-479: local CPU successor to the symbolic center scout

Status: prospectively prepared; no target outcomes at this writing.

Runpod's third recovery create returned an ambiguous HTTP 500. We do not
assume rejection or rent a possible duplicate. Instead, use the existing
NumPy sprinkler implementation locally, retaining the same 551 candidate
inputs, section, eight cycle phases, 8192 seeds, both RK4 steps, horizon,
capture rules, raw event retention, saturation/transversality gates and
downstream analysis as EXP-477. No desired Jones words enter selection.

Only operational execution changes: one candidate per serial batch and a
43,200-second cooperative wall limit. Preserve every completed profile and
any failure; stop at the first invalid batch without automatic retry/resume.
CPU records must be labeled CPU, never GPU. EXP-477 remains unexecuted.

The target-free full-size anchor benchmark took 15.023 and 29.594 seconds
at the two steps, including saving raw arrays, without integration failures.
Its [receipt](receipts/EXP-479-cpu-fallback-benchmark.json) is preserved;
the extrapolation there is a planning estimate, not a measured target runtime.

Before targets, the actual CPU adapter must reproduce the hash-bound existing
64-seed control at both steps, including all seed IDs, checkpoint counts,
event cardinalities and finite state/time errors within the existing `1e-6`
gate. This checks adapter equivalence, **not independent solver replication**.
The qualification receipt must match the execution source and producer hash.

Execute in a detached frozen Git worktree with an explicit copy of only the
candidate input needed from ignored artifacts. No credentials are copied.
A local launchd service with KeepAlive disabled lets collection survive the
chat turn. A task-owned caffeinate process prevents idle system sleep while
the calculation runs; it exits with the calculation. The Mac retains raw
data first. After completion, audit hashes/counts before CPU analysis or
copying the evidence to the authorized prax archive. At least 17,722,933,248
free bytes are required before starting. No paid infrastructure is used.

The outcome is still only an exploratory center nomination. It cannot verify
a Jones itinerary, generating partition, simultaneous critical root, or chain
arrow. Historical-section flow and partition tests remain the next stage.

Implementations: `scripts/run_symbolic_center_cpu.py` and
`scripts/launch_symbolic_cpu.py`; plan:
`experiments/manifests/EXP-479-cpu-symbolic-center-pilot.json`.

Pre-target CLI rehearsal at `b4aa063` saved a passing qualification with exact
zero state/time differences but failed printing NumPy Boolean diagnostics to
stdout. The saved receipt and frozen worktree are retained. The repair uses
the existing NumPy-aware JSON encoder, with a real qualification CLI regression;
a fresh source-bound qualification must exit successfully before targets.
