# EXP-506: complete the historical word adapter without resetting EXP-505

Prospective, outcome-informed engineering continuation of the fixed EXP-505
saved-data sensitivity. No new flow trajectory or parameter is selected.

EXP-505 stopped after retaining all 255 first-profile/x/original branch fits.
Its new adapter compared the raw `_word_row` output against the old completed
receipt, omitting the original runner's subsequent four target-comparison
fields. The original source fills those fields after partition construction.
This is a replay implementation error, not a scientific verdict. The consumed
EXP-505 marker, source, failure and partial evidence remain unchanged.

Complete those four fields using exactly the original logic and hash-bound
source word list, only after raw words have been inferred. Preserve every
original tolerance, probe, bootstrap seed, variant, coordinate and fit rule
from EXP-505. Reuse every one of the 255 completed branch fits, in order,
checking normalized sample hashes, fit identities and outcomes. Never select
a favorable subset. Before new fits, reconstruct the original robust result
from that retained prefix without re-fitting and require agreement with the
historical receipt; separately check the complete word schema on all eight
already stored word rows. No word-slope fit is performed in that preflight.

Then execute the rest of the original two-profile/two-coordinate/two-mode
analysis. First-profile/x/original word-slope fits are not reused: EXP-505
executed ten but failed before retaining their outputs. EXP-506 records all
word-slope spline inputs/coefficients/knots as well as all branch-fit records.
Report branch-oracle and word-slope fit counts separately. A complete analysis
has at most 2,040 branch fits, of which 255 are reused, plus 80 word-slope
spline constructions when every word call reaches the slope stage; unresolved
one-branch partitions may legitimately skip that stage and must be counted.

The continuation has its own source freeze, exclusive marker and fresh output,
a 600-second deadline, 100 MiB new-output limit and 8 GiB free-space floor.
It uses no paid review, rental, raw upload, dependency change or deletion.
Before targets: source/input/failure hash validation, complete-schema controls,
prefix replay with a forbidden fresh-fit sentinel, synthetic restored-helper
and failure-ledger controls, isolated copied-source startup, tests and a clean
live-pushed source. Preserve the original failure even if the continuation
passes. Do not silently amend the frozen EXP-505 implementation.

After execution, hash the entire new output before interpretation. Recompute
all original and filtered branch fits and word-slope splines as an audit from
the authenticated saved EXP-186 arrays, including the reused prefix; compare
the complete results and records, not only headline counts. Those audit fits
are distinct from new target fits and generate no new flow trajectories.

All claim limits from EXP-505 remain: retrospective sensitivity of one saved
experiment, no archive-wide clearance, no new orbit/primitivity certification,
no target-word-driven partition selection and no Jones-arrow verification.
