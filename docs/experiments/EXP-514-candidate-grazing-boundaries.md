# EXP-514: test the section-grazing explanation of all five candidate intervals

Prospective, outcome-informed local diagnostic. EXP-513's full-step projected
fold searches left their u boxes. A read-only examination of the already
audited EXP-512 endpoints and EXP-513 midpoints finds exactly one aligned
section extremum changing sign in the surviving half of each interval, under
both solvers. This motivates a **different equation**: a section tangency,
not another attempt to solve the projected-fold equation.

## Fixed selection and method

Retain all five original intervals. For each, choose the unique half whose
endpoint slopes have opposite signs under both solvers. Require equal extremum
counts, a unique common ordinal with opposite section-plane values, and an
agreed accepted prefix before the extremum. Reject an ambiguous nomination
before any target rather than quietly choosing a favorable event. The candidate
table binds the source endpoints, half, extremum ordinal and four measurements.

Keep the original Rössler parameters, straight initial curve and horizon from
EXP-513. Seed u at the selected half's midpoint and t at the mean of the four
saved extremum times. Confine u to that half and time to the extremum time
range enlarged by .25 on each side. Solve h(u,t)=0 and h_t(u,t)=0 with the
unchanged eight-step bounded grazing shooter. This is event-aware localization,
**not bracket-preserving convergence**: an out-of-box Newton step remains a
search failure and is not retried. No projected-fold thresholds are relaxed.

Require the existing EXP-492 root residual, unfolding, curvature and paired
solver gates. For an eligible pair use its common mean u, then all four signed
offsets: half-width times [-1/1000, -1/10000, 1/10000, 1/1000]. All must fit
the same box. At each input retain a complete guard/main dense census with
both solvers, using the unchanged EXP-513 settings. Keep failed meshes and
continue the prescribed other doses; an unexpected/resource failure terminates
the attempt honestly.

Require the original EXP-492 local birth/death count, accepted prefix,
transversality, uncertain-extremum exclusion and square-root separation-ratio
checks. Compare every accepted event over the entire fixed horizon across
solvers. No late-return disagreement is hidden by truncating at the tangency.
Record the per-dose curve diagnostics even when the projected input is invalid;
regularity of that projection is not necessary for section grazing.

## Permitted conclusions

A pass establishes a **locally numerically qualified section-grazing boundary**
and its two-sided return-event mechanism in the tested interval. It explains
why a scalar derivative sign bracket can be misleading there. It does not
exclude additional smooth folds anywhere in the original interval, provide
an exact-flow proof, restore depth-four fold representations, identify Jones's
C/D dictionary, or verify/debunk a symbolic arrow. All five candidates remain
in the denominator. Failed roots, side failures and disagreement are distinct
from a positive grazing result and from absence.

## Validation, audit and limits

The actual combined root/side controller is exercised by an analytic grazing
positive and bounded no-root negative, with both solvers. Existing target-free
grazing controls cover higher prefixes and malformed/missing side matrices;
replay EXP-511's retained dense-census controls. New tests attack nomination,
scope and complete failed-side execution. Run the authentic isolated consumer,
local design audit, full regression suite and public secret scan before a
clean, committed, pushed and live-verified freeze. No paid review is requested.

One target attempt, at most 160 IVPs: five times (2 solvers times 8 shooting
steps + 4 doses times 2 solvers times 2 guard/main integrations). Maximum
3,600 seconds, 2 GiB including final summary, initial free space 11 GiB,
continuing floor 8 GiB, failure reserve 1 MiB. All writers enforce the cap
before writing. No new workers, raw uploads or old-attempt resets.

Audit all new shooting meshes against separately coded Rössler grazing algebra
and every side's dense polynomials/guard/event census, then replay every
qualification and complete accounting. This is a same-agent local audit,
not independent-team replication. Historical inputs remain compact reused
evidence, not a second audit of all their raw trajectories. Release compact
results and truthful replay scope; retain raw locally.
