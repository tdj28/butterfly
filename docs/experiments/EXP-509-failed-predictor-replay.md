# EXP-509: forensic replay of the failed EXP-508 predictor

## Question and prior exposure

Can the saved predictor be fully audited, and can an incomplete contact be
reported as an unqualified point without crashing or licensing a corrector?
This is an outcome-informed software correction and raw replay, not another
flow experiment or a successful completion of EXP-508.

Before this freeze we observed the TypeError, 196 consumed IVPs, a saved
unqualified predictor, a qualified cycle, complete boundary analysis, qualified
depth-4 folds and unqualified depth-8 folds. The complete raw inventory was
hash-checked. Detailed fold failure causes and numerical comparisons have not
yet been reinterpreted. Those observations motivate this amendment; it is not
a blind confirmation.

## Immutable inputs and correction

EXP-508 source is 709af03fe9a8f6c6d9b5480a886cb7d0a2c3ce5a. Its complete raw
directory, failure, point, consumed marker and all 87 source files remain
unchanged. Exact failure, point and marker hashes are fixed in the machine
plan. No original summary or comparison will be manufactured. Its progress
still says predictor started, corrector not-run: the crash occurred before the
controller recorded completion, after the point producer finished.

The new qualification helper treats absent, malformed, nonfinite or negative
contact-distance arrays as ineligible. Valid arrays and all original cycle
correspondence and 1e-4 proximity rules retain their meaning. A copied function
environment substitutes only that helper into the old controller; no frozen
module is patched in place. The reporting result is explicitly reconstructed.

## Execution and checks

Before raw replay, commit and push the source, tests, this protocol and its
machine plan; verify the exact live remote revision. Synthetic controls cover
the missing-envelope failure, misleading qualified flags, nonfinite and
malformed values, valid-path equivalence, unchanged old failure, and absence
of global mutations even when an evaluator raises. Run the existing suite.

The sole replay authenticates the entire original inventory before scientific
interpretation, checks the original source/startup/marker and analytic control
receipts, and invokes the existing complete point auditor. That auditor
recomputes fold qualification, solver comparisons, periodic closure and
primitive counts, guard continuity, Decimal boundary shooting, full prefix
censuses, all distances and the point decision from retained meshes and
coefficients. Reconcile all 196 IVPs and all retained files. Hash-check the
original again afterward. No IVP, new target, corrector or raw upload is allowed.

One fresh replay marker and output namespace; 3,600 seconds and 16 MiB total
output including the result, with a 1 MiB failure reserve. If the replay fails,
retain that failure. Do not reset either experiment. Report every fold and
boundary, not only qualified cases, and publish the failure separately from
the reconstructed numerical rejection. Public compact evidence is not a
replacement for the full local raw archive. Shared-code local audit is not
independent-team replication.

## Claim boundary and next action

No accepted continuation, exact contact, symbolic C/D assignment, p-to-p+1
chain, global proof or Jones debunk follows from a reporting repair. Diagnose
the audited depth-8 failures before selecting another scientific target.
Numerical tolerances, branch identities and parent ledger remain unchanged.
Routine local repair needs no paid Pro review; none is requested or authorized.
