# EXP-512: extend all seven time-censored return profiles

Prospective, outcome-informed local diagnostic. No target run at freeze;
no paid review, cloud worker or new upload. EXP-511 remains unchanged.

## Fixed question and matrix

EXP-511 completed every solve, but seven samples had eight rather than nine
accepted returns before their declared horizon. These are censored finite
observations, not proof of absent returns or holes in a return domain.
Extend **all seven**, not a favorable subset: direction 0 nodes 1,16,17,18,19
and direction 1 nodes 18,19, both DOP853 and Radau. Keep every initial state,
tangent, u, parameter, tolerance and event ordinal unchanged. Increase each
horizon by exactly 15 time units, from about 55.67 to about 70.67.

Fourteen full profiles, guard plus main each: at most 28 new IVPs. This is a
fresh experiment, not continuation of the consumed EXP-511 marker. There
are no new curve nodes, Newton refinements, cycles or boundary trajectories.
Reintegrate from the unchanged initial data so both the old prefix and the
extension are retained together; never splice unverified trajectory pieces.

## Acceptance and explicit historical reuse

At each new profile, compare **all eight** accepted events before the old
horizon against the same method's old report: scaled state <=1e-6, time
<=1e-7, relative scaled raw tangent <=1e-6. An extra/missing accepted event,
earlier uncertain extremum, nonfinite value or prefix disagreement rejects
admission even if the new ninth event looks favorable. Dense replay checks
new event states/tangents and their corrected curve measurements separately.

Create a newly labeled mixed-provenance diagnostic grid only: 33 unchanged
EXP-511 paired samples plus seven extended pairs. The two previously
collapsed input samples remain unchanged and rejected. Recompute the entire
same-grid pair/coverage/bracket analysis. If a prefix fails, retain its profile
but prevent its pair from qualifying. Track both all-prefixes-preserved and
all-ninth-returns-observed; neither controller completion nor prefix agreement
alone establishes complete coverage. Original EXP-511 is never relabeled.

An opposite-sign endpoint pair is only a numerical candidate, not certified
continuity across the u interval. Event-sheet changes, grazing cuts, input
turns and unsampled intervals remain possible. New ninth returns do not
verify a fold, critical symbol, generating partition or Jones chain.

## Evidence, controls and resource admission

Reuse the byte-identical EXP-511 capture, bounded producer and dense replay
functions. Replay its six saved analytic profiles (12 old control IVPs, zero
new control integrations). New target-free tests cover the exact seven-node
selection, prefix changes/omissions, uncertainty, failed integration,
complete assembly and prevention of false ninth-return promotion. Validate
the actual extension consumer in an isolated copied source/input closure.
Run the full regression suite before freezing tested sources and this plan,
then push and live-verify the exact execution ref before the single target.

Retain each guard/main mesh, full main dense coefficients and solver event
lists before success checks, plus starts, profiles, prefix comparisons and
the complete newly labeled grid. Audit all new dense products, reconcile
the complete inventory and call count, and separately replay scalar grid
distances/bracket decisions. Explicitly distinguish the new raw audit from
reuse of EXP-511's authenticated public receipt. The latter does not repeat
its old 160-IVP raw audit.

Target limits: 28 IVPs, 900 seconds, 512 MiB total output including summary,
9 GiB initial free space, 8 GiB continuing floor, 1 MiB failure reserve.
Pre-write admission covers every JSON/NPZ producer and final summary.
Preserve raw prefixes and a failure receipt on budget or unexpected errors;
never reset the exclusive marker. Solver failures are retained as explicit
unqualified profiles while the remaining independent fixed samples execute.

Release the full local audit, a compact public replay with tamper tests, and
an update separating censoring resolution from genuine fold identification.
Merge only after all final-head push and PR Python 3.12/3.13 checks pass.

Verification ledger at freeze: shared numerical producers have EXP-511
analytic/raw validation; new selection/prefix/controller wiring is
target-free-tested and isolated-startup-tested. The new target has not run.
