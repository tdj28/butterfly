# EXP-496 local pre-outcome audit

2026-09-09. Same-agent audit, not independent peer review. No paid review is
requested or used. The prospective endpoint protocol and generated machine
inputs are distinct from the consumed EXP-490/494/495 runs.

The deterministic public-input builder retains all 26 parent identities,
selects eight representatives before new target outcomes, and retains two
unselected duplicate representations plus sixteen ineligible candidates.
The lower endpoint is exactly EXP-494's qualified negative-a step 3, with
primitive six/eight counts, correction, paired and repeat-window gates checked.
No new periodic observation is integrated or rephased.

The production numerical functions and tolerances are reused unchanged from
EXP-490. New initial section offsets, seeds, boxes and horizons are explicit
in the generated candidate table. Both new solvers receive the same averaged
old root seed. Source closure includes the eagerly imported butterfly modules
and the separate auditor; execution checks it against the static manifest.
The public audit needs no private attempt marker and performs zero new IVPs.

The 15 focused successor tests pass. They check complete selection, section
transport, paired seeds, primitive/repeat/correction/phase gates, separate
scalar distance and sign arithmetic, fixed event 3, mixed outcomes, missing
variants, changed input/source bindings, and result-anchor rejection. Together
with the EXP-490/495 focused tests, 46 tests pass.

All twelve actual analytic controls passed through production shooting and
event-census code, and their analytic-root/curvature audit passed. The local
receipt is `artifacts/EXP-496/preflight-controls-01.json`, SHA-256
`cc0de1e4a36a2d2538453598acbfe862fdaecd0c7f38d17cb832a58dfbf15700`.
The runtime repeats these controls before creating the consumed target marker.

The first full-suite run had 2134 passes, seven failures, eight setup errors
and eleven skips. Its existing process-supervision controls could not invoke
`/bin/ps` under the restricted host environment; the retained terminal witness
reported `PermissionError: Operation not permitted`. No numerical threshold
was changed or test disabled. The separate host-access full suite passed
**2159 tests with one existing Linux-only skip**, in 137.70 seconds. Its receipt
is `artifacts/EXP-496/preflight-tests-02.xml`. The additional ten restricted-host
skips ran successfully with process-inspection access. No production source
change was needed to resolve those test failures.

No EXP-496 target attempt has been consumed at this preflight checkpoint.
