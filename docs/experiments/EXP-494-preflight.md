# EXP-494 pre-outcome local audit

September 9, 2026. No paid review requested: this is a bounded successor
within the existing research question, governed by AGENTS.md.

The scientific distinction under test is periodic-family transport, not
another initial-condition sweep. Both known candidates remain in scope, with
four independent parameter arms each and all 64 planned new parameter nodes.
The same paired seed is used by both methods; an unqualified pair cannot feed
a successor. A lost branch remains unresolved, not an absence result.

Local adversarial pass: a repeated traversal could masquerade as an extra
period; accepted-event counts could be substituted for geometry; an ordinary
event list could omit a close crossing; skipped continuations could disappear
from the result; or paired solvers could use incompatible phases. The design
addresses these respectively with all gcd-divisor checks and distinct event
states; independently summed polygon angles; extremum-bracketed roots plus
retained dense polynomials; a complete 66-node ledger (including two bases);
and a common preceding phase reference. These are numerical checks, not
validated enclosures or an independent research team.

All four actual circular-orbit controls pass in
`artifacts/EXP-494/preflight-controls-03/controls.json`, SHA-256
`aeaf865a58c5ca20671ce83d728636f7aa070dff06c58e9e68a77de49a02f4ea`.
Both integrators accept one traversal and reject two traversals as a minimal
period; the double traversal still correctly has winding two. The 35 focused
tests additionally exercise observer/coefficient replay, membership tampering,
missing methods, failed bases, blocked arms, budget interruption, and retained
failed corrector meshes. Controls 01/02 are preserved as earlier preflight
artifacts; final production runs the current control consumer again.

Pre-outcome failures were not scientific target failures. The first full
suite encountered the host sandbox's `PermissionError` for `/bin/ps` in old
supervisor controls: 7 failures, 8 setup errors, 2068 passes, 11 skips. A
host-access run removed those failures but caught an undefined local `period`
variable in four newly added dense-replay tests. That test defect was fixed;
its original JUnit report remains at `artifacts/EXP-494/preflight-tests.xml`.
The first sandbox failure's temporary files expired under pytest retention
before a later attempted copy; its tool output survives in the task history,
not as a newly reconstructed raw receipt. No target data had been opened.

Final full suite: **2096 passed, 1 Linux-only skip in 135.99 seconds**;
`artifacts/EXP-494/preflight-tests-02.xml`. Both public CLIs import and show
help, and compilation passes. This qualifies the local execution path, not
any Rössler geometry or symbolic word. No optional paid review blocks launch.
